name: Orka Ephemeral Runner Demo

on:
  workflow_dispatch:

jobs:
  demo-job:
    runs-on: [self-hosted, orka-runner]
    steps:
      - name: Check macOS version
        run: sw_vers
      - name: Echo success
        run: echo "Running on Orka ephemeral macOS runner!"
