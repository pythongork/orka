name: Orka Ephemeral Runner Dem0

on: [push]

jobs:
  demo-job:
    runs-on: orka-ephemeral-runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run a command
        run: |
          echo "Hello world from Orka ephemeral runner!"
          uname -a
