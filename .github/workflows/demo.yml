name: Orka Ephemeral Runner Demo

on: [push]

jobs:
  demo-job:
    runs-on: my-orka-runner
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run a command
        run: |
          echo "Hello from Orka ephemeral runner!"
          uname -a
